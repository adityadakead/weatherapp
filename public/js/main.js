const submitButton = document.getElementById('submitButton');
const check = document.getElementById('check');

const getInfo =(event)=>{
    event.preventDefault();
    alert("jdjf");
}

check.addEventListener('click',getInfo);